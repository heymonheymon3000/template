object AndroidX {
        private const val coreKtxVersion = "1.8.0"
        const val coreKtx = "androidx.core:core-ktx:$coreKtxVersion"

        private const val appCompatVersion = "1.5.0"
        const val appCompat = "androidx.appcompat:appcompat:$appCompatVersion"

        private const val lifecycleVmKtxVersion = "2.5.1"
        const val lifecycleVmKtx =
            "androidx.lifecycle:lifecycle-viewmodel-ktx:$lifecycleVmKtxVersion"
        const val lifecycleRunTimeKtx =
            "androidx.lifecycle:lifecycle-runtime-ktx:$lifecycleVmKtxVersion"

        private const val datastoreVersion = "1.0.0"
        const val datastoreCore = "androidx.datastore:datastore-preferences-core:$datastoreVersion"
        const val datastore = "androidx.datastore:datastore-preferences:$datastoreVersion"

        private const val windowVersion = "1.0.0"
        const val window = "androidx.window:window:$windowVersion"

        private const val biometricVersion = "1.1.0"
        const val biometric = "androidx.biometric:biometric:$biometricVersion"

        private const val constraintlayoutVersion = "2.1.4"
        const val constraintlayout =
            "androidx.constraintlayout:constraintlayout:$constraintlayoutVersion"

        private const val webkitVersion = "1.4.0"
        const val webkit = "androidx.webkit:webkit:$webkitVersion"

        private const val camera_coreVersion = "1.1.0"
        const val camera_core = "androidx.camera:camera-core:$camera_coreVersion"

        private const val camera_camera2Version = "1.1.0"
        const val camera_camera2 = "androidx.camera:camera-camera2:$camera_camera2Version"

        private const val camera_lifecycleVersion = "1.1.0"
        const val camera_lifecycle = "androidx.camera:camera-lifecycle:$camera_lifecycleVersion"

        private const val camera_viewVersion = "1.1.0"
        const val camera_view = "androidx.camera:camera-view:$camera_viewVersion"

        private const val camera_videoVersion = "1.1.0"
        const val camera_video = "androidx.camera:camera-video:$camera_videoVersion"

        private const val camera_extensionsVersion = "1.1.0"
        const val camera_extensions = "androidx.camera:camera-extensions:$camera_extensionsVersion"

        private const val concurrent_futuresVersion = "1.1.0"
        const val concurrent_futures =
            "androidx.concurrent:concurrent-futures-ktx:$concurrent_futuresVersion"

        const val navigation_version = "2.5.3"
        const val navigation_dynamic_features =
            "androidx.navigation:navigation-dynamic-features-fragment:$navigation_version"
        const val navigation_fragment =
            "androidx.navigation:navigation-fragment-ktx:$navigation_version"
        const val navigation_ui = "androidx.navigation:navigation-ui-ktx:$navigation_version"
        const val navigation_compose = "androidx.navigation:navigation-compose:$navigation_version"
        const val navigation_testing = "androidx.navigation:navigation-testing:$navigation_version"
        const val navigation_ui_ktx = "androidx.navigation:navigation-ui-ktx:$navigation_version"

        private const val slf4jVersion = "1.7.36"
        const val slf4j_simple = "org.slf4j:slf4j-simple:$slf4jVersion"
        const val slf4j_api = "org.slf4j:slf4j-api:$slf4jVersion"
    }

    object AndroidXTest {
        private const val version = "1.3.0"
        const val runner = "androidx.test:runner:$version"
    }



